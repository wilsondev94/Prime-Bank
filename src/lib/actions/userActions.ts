"use server";

export async function signIn() {
  try {
  } catch (error) {
    console.error("error", error);
  }
}

export async function signUp(uerData: SignUpParams) {
  try {
  } catch (error) {
    console.error("error", error);
  }
}
